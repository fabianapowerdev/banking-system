spring:
  datasource:
    url: ${HML_DB_URL}
    username: ${HML_DB_USERNAME}
    password: ${HML_DB_PASSWORD}

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    root: INFO

